## What changed and why

<!-- Summarize the change and the motivation behind it. Link any related issue. -->

## How this was tested

<!-- e.g. added/updated unit tests, added/updated integration tests against the fixture server,
     manually ran `npm run wp -- ...` against a real site. -->

## Checklist

- [ ] `npm test` passes (unit + integration suites)
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] Added or updated tests for the change (see `CLAUDE.md` for testing conventions)
- [ ] Updated documentation (README, `--help` text, `CLAUDE.md`) if behavior or architecture changed
